# 🤖 Discord AI 비서 봇

Claude AI 기반 개인 비서 봇 — 운동 코치 / 식단 어드바이저 / 번역 / 일정 관리 / 만능 비서

---

## ✨ 기능

- 디스코드 채널 이름에 따라 **AI 역할 자동 전환**
- `AsyncAnthropic` 비동기 클라이언트로 빠른 응답
- **SQLite** 기반 대화 히스토리 영속 저장 (재시작해도 유지)
- `/저장` 커맨드로 오늘 일지 AI 요약 후 **파일 & Notion** 동시 저장
- **Notion DB** 연동 — 헬스 일지 / 할일 / 번역 기록 / 메모
- **Google Calendar** 연동 — 자연어로 일정 추가, 오늘·이번 주 조회, 자동 감지
- 2000자 초과 메시지 자동 분할 전송

---

## 📺 채널별 모드

| 채널명 | 모드 | AI 역할 | 사용 모델 |
|--------|------|---------|-----------|
| `#헬스` | 헬스 트레이너 + 식단 어드바이저 💪 | 운동 루틴, 무게 피드백, 칼로리/영양 조언 + `/저장` 시 Notion 헬스 일지 저장 | claude-haiku-4-5-20251001 |
| `#번역` | 번역 어시스턴트 🌏 | 한/중/영 번역 + 병음 제공 + Notion 자동 저장 | **claude-sonnet-4-6** |
| `#일정` | 일정 관리 비서 📅 | 할일 정리, 데드라인 관리 + 자연어 일정 캘린더 자동 추가 | claude-haiku-4-5-20251001 |
| 그 외 | 만능 비서 🤖 | 게임, 개발, 일상 질문 | claude-haiku-4-5-20251001 |

---

## ⚙️ 환경변수 설정

| 변수명 | 필수 여부 | 설명 |
|--------|----------|------|
| `DISCORD_TOKEN` | **필수** | 디스코드 봇 토큰 |
| `ANTHROPIC_API_KEY` | **필수** | Anthropic API 키 |
| `NOTION_TOKEN` | 선택 | Notion 통합 토큰 |
| `NOTION_HEALTH_DB_ID` | 선택 | Notion 헬스 일지 DB ID |
| `NOTION_TODO_DB_ID` | 선택 | Notion 할일 DB ID |
| `NOTION_TRANSLATION_DB_ID` | 선택 | Notion 번역 기록 DB ID |
| `NOTION_MEMO_DB_ID` | 선택 | Notion 메모 DB ID |
| `GOOGLE_CALENDAR_ID` | 선택 | Google Calendar ID |
| `GOOGLE_CREDENTIALS_JSON` | 선택 | 서비스 계정 JSON (한 줄 문자열) |

> ⚠️ 필수 환경변수(`DISCORD_TOKEN`, `ANTHROPIC_API_KEY`)가 없으면 봇이 시작 시 오류와 함께 종료됩니다.

---

## 💬 커맨드

### ⚙️ 기본 커맨드
| 커맨드 | 설명 |
|--------|------|
| `/저장` | 오늘 대화 AI 요약 후 파일 & Notion 저장 |
| `/초기화` | 이 채널 대화 히스토리 삭제 |
| `/히스토리` | 현재 저장된 대화 수 확인 |
| `/모드` | 현재 채널 모드 및 사용 모델 확인 |
| `/도움말` | 전체 사용법 출력 |

### 📅 일정 커맨드 (Google Calendar)
| 커맨드 | 설명 |
|--------|------|
| `/일정추가 [내용]` | 자연어로 일정 파싱 후 캘린더에 추가 (예: `/일정추가 내일 오후 3시 치과`) |
| `/오늘일정` | 오늘 구글 캘린더 일정 조회 |
| `/이번주일정` | 이번 주 일정 조회 |

### ✅ 할일 커맨드 (Notion)
| 커맨드 | 설명 |
|--------|------|
| `/할일추가 [내용]` | Notion 할일 DB에 추가 |
| `/할일목록` | 미완료 할일 목록 조회 (마감일·우선순위 표시) |
| `/할일완료 [할일명]` | 해당 할일 완료 처리 |

### 📝 메모 커맨드 (Notion)
| 커맨드 | 설명 |
|--------|------|
| `/메모 [제목] \| [내용]` | Notion 메모 DB에 저장 (`|` 구분자로 제목/내용 분리) |

---

## 📓 Notion DB 구조

| DB 이름 | 환경변수 | 필요 프로퍼티 |
|---------|---------|--------------|
| 헬스 일지 | `NOTION_HEALTH_DB_ID` | `이름` (제목), `날짜` (날짜) |
| 할일 | `NOTION_TODO_DB_ID` | `이름` (제목), `완료` (체크박스), `우선순위` (선택), `마감일` (날짜) |
| 번역 기록 | `NOTION_TRANSLATION_DB_ID` | `원문` (제목), `번역` (텍스트), `날짜` (날짜) |
| 메모 | `NOTION_MEMO_DB_ID` | `제목` (제목), `내용` (텍스트), `날짜` (날짜) |

---

## 🚀 실행 방법

### 로컬 실행
```bash
pip install -r requirements.txt

# .env 파일에 환경변수 설정 후
python bot.py
```

### Railway 배포
1. GitHub 레포 연결
2. Railway Variables에 환경변수 입력
3. 자동 배포 완료

---

## 📦 의존성

```
discord.py==2.4.0
anthropic>=0.40.0
notion-client>=2.2.1
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.8.0
python-dotenv>=1.0.0
```

Python **3.11.9** 권장
